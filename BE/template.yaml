AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: TaskHub Spring Boot backend on AWS Lambda and API Gateway

Parameters:
  SpringDatasourceUrl:
    Type: String
    Description: Managed PostgreSQL JDBC URL
  SpringDatasourceUsername:
    Type: String
    Description: Managed PostgreSQL username
  SpringDatasourcePassword:
    Type: String
    NoEcho: true
    Description: Managed PostgreSQL password
  AppJwtSecret:
    Type: String
    NoEcho: true
    MinLength: 32
    Description: JWT signing secret, minimum 32 characters
  SupabaseUrl:
    Type: String
    Default: ''
    Description: Optional Supabase project URL for Storage integration
  SupabaseServiceRoleKey:
    Type: String
    NoEcho: true
    Default: ''
    Description: Optional Supabase service role key for Storage integration
  AppMailDeliveryEnabled:
    Type: String
    Default: 'true'
    AllowedValues: ['true', 'false']
    Description: Enable AWS SES security emails
  AppMailFromEmail:
    Type: String
    Description: Verified SES sender address; required when mail delivery is enabled

Globals:
  Function:
    Runtime: java21
    Architectures:
      - x86_64
    MemorySize: 3008
    Timeout: 60
    Tracing: Active

Resources:
  ServerlessApi:
    Type: AWS::Serverless::Api
    Properties:
      StageName: Prod
      MethodSettings:
        - ResourcePath: '/*'
          HttpMethod: '*'
          ThrottlingBurstLimit: 100
          ThrottlingRateLimit: 50

  TaskHubBackendFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: taskhub-backend
      CodeUri: target/taskhub-backend-1.0.0-lambda.jar
      Handler: com.taskhub.lambda.StreamLambdaHandler::handleRequest
      Policies:
        - Statement:
            - Effect: Allow
              Action:
                - bedrock:InvokeModel
              Resource: '*'
            - Effect: Allow
              Action:
                - ses:SendEmail
              Resource: '*'
      Environment:
        Variables:
          SPRING_PROFILES_ACTIVE: supabase,prod
          SPRING_DATASOURCE_URL: !Ref SpringDatasourceUrl
          SPRING_DATASOURCE_USERNAME: !Ref SpringDatasourceUsername
          SPRING_DATASOURCE_PASSWORD: !Ref SpringDatasourcePassword
          SPRING_JPA_HIBERNATE_DDL_AUTO: validate
          SPRING_JPA_SHOW_SQL: 'false'
          SPRING_FLYWAY_ENABLED: 'true'
          SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE: '4'
          SPRING_DATASOURCE_HIKARI_MINIMUM_IDLE: '0'
          SPRING_DATASOURCE_HIKARI_CONNECTION_TIMEOUT: '30000'
          JAVA_TOOL_OPTIONS: -XX:+TieredCompilation -XX:TieredStopAtLevel=1
          TZ: Asia/Ho_Chi_Minh
          APP_JWT_SECRET: !Ref AppJwtSecret
          APP_WALLET_CASH_SIMULATION_ENABLED: 'false'
          APP_BEDROCK_ENABLED: 'true'
          APP_BEDROCK_REGION: us-east-1
          APP_BEDROCK_MODEL_ID: global.anthropic.claude-haiku-4-5-20251001-v1:0
          APP_BEDROCK_TEMPERATURE: '0.3'
          APP_BEDROCK_MAX_TOKENS: '4096'
          APP_CORS_ALLOWED_ORIGINS: https://taskhubvn.com,https://www.taskhubvn.com,https://main.d3lqxlf1rfgnci.amplifyapp.com
          APP_FRONTEND_BASE_URL: https://taskhubvn.com
          APP_REQUIRE_EMAIL_VERIFICATION: 'true'
          APP_REFRESH_COOKIE_SECURE: 'true'
          APP_REFRESH_COOKIE_SAME_SITE: None
          APP_ADMIN_ENABLED: 'false'
          APP_MAIL_DELIVERY_ENABLED: !Ref AppMailDeliveryEnabled
          APP_MAIL_FROM_EMAIL: !Ref AppMailFromEmail
          APP_MAIL_REGION: us-east-1
          SUPABASE_URL: !Ref SupabaseUrl
          SUPABASE_SERVICE_ROLE_KEY: !Ref SupabaseServiceRoleKey
      Events:
        Warmup:
          Type: Schedule
          Properties:
            Schedule: rate(5 minutes)
            Enabled: true
            Input: '{"source":"taskhub.warmer","action":"keep-warm"}'
        Api:
          Type: Api
          Properties:
            RestApiId: !Ref ServerlessApi
            Path: /{proxy+}
            Method: ANY
        RootApi:
          Type: Api
          Properties:
            RestApiId: !Ref ServerlessApi
            Path: /
            Method: ANY

Outputs:
  ApiUrl:
    Description: API Gateway endpoint URL
    Value: !Sub 'https://${ServerlessApi}.execute-api.${AWS::Region}.amazonaws.com/Prod/'
